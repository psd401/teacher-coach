{
  "name": "teacher-coach-api",
  "version": "1.0.0",
  "description": "Backend API for Teacher Coach - handles auth validation, Claude API proxy, and rate limiting",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "wrangler": "^4.59.2"
  }
}
